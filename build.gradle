apply plugin: 'java'

configurations {
    cucumberRuntime {
        extendsFrom testRuntime
    }
}

task purgomalum_test() {
    dependsOn assemble, compileTestJava
    doLast {
        javaexec {
            main = "cucumber.api.cli.Main"
            classpath = configurations.cucumberRuntime + sourceSets.main.output + sourceSets.test.output
            args = ['--plugin', 'pretty', '--glue', 'com.letgo.test.purgomalum', 'src/test/resources']
        }
    }
}

dependencies {
    testCompile 'io.cucumber:cucumber-java:2.1.0'
    testCompile 'io.cucumber:cucumber-junit:2.1.0'
    testCompile 'org.json:json:20171018'


    testCompile 'junit:junit:4.12'
}

repositories {
    mavenCentral()
}

